services:
  aws-mcp:
    build: .
    container_name: aws-mcp-server
    ports:
      - "8000:8000"
    volumes:
      # Chemin relatif Windows avec des SLASH (/) (à adapter selon votre OS)
      - ${USERPROFILE}/.aws:/root/.aws:ro
    restart: unless-stopped
